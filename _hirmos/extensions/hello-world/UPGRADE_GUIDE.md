# hello-world Upgrade Guide

## Upgrading to 1.0.0

Initial stable version baseline for `hello-world`.

### Required actions
- None for a fresh install.

### Notes
- `hello-world` now declares its own `version` in `extension.yaml`.
- `hello-world` now declares framework compatibility through `requires_core` in `extension.yaml`.
