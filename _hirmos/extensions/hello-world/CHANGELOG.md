# hello-world Changelog

## [1.0.0] - 2026-04-01

### Added
- Initial stable version baseline for `hello-world`.
- Public support for the minimal runnable extension baseline.
- default entrypoint

### Changed
- Established extension-level versioning and core compatibility through `version` and `requires_core` in `extension.yaml`.
