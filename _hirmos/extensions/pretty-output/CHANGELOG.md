# pretty-output Changelog

## [1.0.0] - 2026-04-01

### Added
- Initial stable version baseline for `pretty-output`.
- Public support for the minimal hook-only extension baseline.
- hello-world.after-main-output hook subscription

### Changed
- Established extension-level versioning and core compatibility through `version` and `requires_core` in `extension.yaml`.
