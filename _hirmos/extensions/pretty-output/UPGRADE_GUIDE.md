# pretty-output Upgrade Guide

## Upgrading to 1.0.0

Initial stable version baseline for `pretty-output`.

### Required actions
- None for a fresh install.

### Notes
- `pretty-output` now declares its own `version` in `extension.yaml`.
- `pretty-output` now declares framework compatibility through `requires_core` in `extension.yaml`.
