# Extending Existing Extensions

The preferred customization path is to extend an existing extension when the behavior is additive or compositional.

Forking an extension should be the fallback when the goal is to replace its philosophy or core method.
