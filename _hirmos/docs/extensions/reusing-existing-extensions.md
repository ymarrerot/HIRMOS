# Reusing Existing Extensions

Reuse existing extensions when you want a custom flow built from already-shipped capabilities.

Prefer composing with public entrypoints over copying an extension family.
