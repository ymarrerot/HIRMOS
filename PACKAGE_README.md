# HIRMOS core framework package

This package contains the minimal HIRMOS Core, framework docs, shared artifact working folders, stack definitions, extension-authoring materials, and bundled example extensions.

Bundled example extensions:
- `hello-world`
- `pretty-output`

Separated official/private extensions are not included here. Install them by overlaying their package `_hirmos/` folders into this Core package.
